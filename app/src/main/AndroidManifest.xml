<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bits.dissertation.project.fltcargoplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FlightPlannerActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".CargoListActivity"
            android:label="CargoListActivity">

        </activity>

        <activity android:name=".FlightSearchActivity"
            android:label="FlightSearchActivity"></activity>

        <activity android:name=".FlightSearchResultActivity"
            android:label="FlightSearchResultActivity"></activity>

        <activity android:name=".FlightResultDetailsActivity"
            android:label="FlightResultDetailsActivity"></activity>

        <activity android:name=".MainActivity"
            android:label="MainActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity android:name=".LogInActivity"
            android:label="LogInActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".SignUpActivity"
            android:label="SignUpActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

    </application>

</manifest>
